// eslint-disable-next-line @typescript-eslint/no-var-requires
const Typed = require('typed.js');

// Typed.js is a library that types, in this case types "Galeed Gutierrez"
new Typed('.g-home__my-name', {
	strings: ['Galeed Gutierrez'],
	startDelay: 500,
	typeSpeed: 60,
});
